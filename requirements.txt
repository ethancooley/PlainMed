# Inference + app (installed on Hugging Face Spaces / Render)
transformers>=4.46,<5
peft>=0.13
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
textstat>=0.7
accelerate>=1.0

# Training only (needed on Colab, not required to serve inference):
#   trl>=0.12
#   datasets>=3.0
#   scikit-learn>=1.5
# torch is installed separately (CPU wheel in Docker, GPU wheel on Colab).
